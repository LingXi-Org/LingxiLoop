---
name: agent-runtime
description: Change LingxiLoop's published LingxiOS integration, tool authorization, messages, or LLM ledger. Use for server/src/agent-runtime and public runtime contracts.
---

# Agent runtime

Trace ingress → published LingxiOS worker → model turn → native tool or guarded Python → product authorization → ledger → streamed preview and durable final message before editing.

- Use the exact published LingxiOS dependency through public exports. Native business tools are model-visible according to current grants; guarded `ipython` is optional. Product effects repeat server-side authorization for the acting user, company, project, run and frozen audience. Do not restore the retired Agent OS or a compatibility adapter.
- Preserve leases, bounded retries, idempotency, stale-owner rejection, WuKongIM as durable messages, and kernel/home isolation.
- Product LLM paths use the shared client; LingxiOS owns runtime model calls and reports them through the shared LLM ledger. Never log prompts or secrets. Keep provider thinking disabled for main and auxiliary calls, and only final content in user text previews.
- Add deterministic affected integration coverage and real provider regression for model/protocol changes. Run `server:typecheck` and owning tests; use only validation scripts that exist in the current package metadata. Do not use Playwright.
